// throws if gltfResource is undefined
// uses provided baseResource
// uses provided typedArray
// keeps glTF JSON and embedded buffers resident on the CPU
// loads glTF
// Check that components are defined
// loads glTF resources asynchronously
// becomes ready before textures are loaded when incrementallyLoadTextures is true

// throws if resourceCache is undefined
